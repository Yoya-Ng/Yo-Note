---
import { getIndex } from "@lib/contentParser";
import PageLayout from "@components/calendar/PageLayout.astro";
import type { CalendarEntry } from "@/types";

// const entry = (await getIndex("calendar")) as CalendarEntry;
---

<PageLayout />
